name: cocktailbot_app
description: CocktailBot Raspberry Pi kiosk application
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^11.0.2
  http: ^1.6.0
  qr_flutter: ^4.1.0
  shared_preferences: ^2.5.5

flutter:
  uses-material-design: true
  assets:
    - assets/drinks/
    - assets/drinks/cocktailbot/
